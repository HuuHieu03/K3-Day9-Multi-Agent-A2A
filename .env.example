# API Keys & LLM Configuration (Do NOT commit real keys to git)
# Note: Model must be <= 10B parameters as per lab constraints.

LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai.com/v1 # or http://localhost:11434/v1 for local Ollama / vLLM
LLM_MODEL_NAME=qwen2.5:7b # or other model <= 10B parameters
